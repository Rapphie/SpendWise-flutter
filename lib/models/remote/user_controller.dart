import 'package:flutter/material.dart';

class UserControllerRemote extends StatelessWidget {
  const UserControllerRemote({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
